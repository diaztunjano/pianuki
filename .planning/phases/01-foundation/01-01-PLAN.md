---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tailwind.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/lib/noteUtils.ts
  - src/lib/supabase.ts
  - src/stores/audioStore.ts
  - src/stores/index.ts
  - src/components/AppShell.tsx
  - src/components/GameCanvas.tsx
  - src/components/KeyboardStrip.tsx
  - src/components/DebugPanel.tsx
  - .env.local
  - .gitignore
autonomous: true
requirements:
  - AINP-03

must_haves:
  truths:
    - "Game canvas renders an L/U-shape path with placeholder visuals (colored shapes, background) in a Chrome browser filling the window"
    - "88-key keyboard strip is visible at the bottom of the screen and can highlight keys when activeNotes changes in the store"
    - "Debug panel is visible on the right side in development mode and shows events from the store"
    - "NoteOn/NoteOff event type and Zustand store exist so audio hooks can dispatch unified events"
  artifacts:
    - path: "src/stores/audioStore.ts"
      provides: "Zustand audio slice with activeNotes Set, events array, addEvent action"
      contains: "addEvent"
    - path: "src/components/GameCanvas.tsx"
      provides: "Canvas 2D game rendering with rAF loop and ResizeObserver"
      contains: "requestAnimationFrame"
    - path: "src/components/KeyboardStrip.tsx"
      provides: "88-key piano display reading activeNotes from store"
      contains: "activeNotes"
    - path: "src/components/DebugPanel.tsx"
      provides: "Dev-only event log reading events from store"
      contains: "import.meta.env.DEV"
    - path: "src/components/AppShell.tsx"
      provides: "CSS Grid layout: canvas left/center, debug right, keyboard bottom"
      contains: "GameCanvas"
    - path: "src/lib/noteUtils.ts"
      provides: "frequencyToMidiNote, midiNoteToName, isInPianoRange pure functions"
      exports: ["frequencyToMidiNote", "midiNoteToName", "isInPianoRange"]
    - path: "vite.config.ts"
      provides: "Vite config with React plugin, Tailwind v4 plugin, path alias, COOP/COEP headers"
      contains: "@tailwindcss/vite"
  key_links:
    - from: "src/components/KeyboardStrip.tsx"
      to: "src/stores/audioStore.ts"
      via: "useBoundStore selector for activeNotes"
      pattern: "useBoundStore.*activeNotes"
    - from: "src/components/DebugPanel.tsx"
      to: "src/stores/audioStore.ts"
      via: "useBoundStore selector for events"
      pattern: "useBoundStore.*events"
    - from: "src/components/GameCanvas.tsx"
      to: "src/stores/audioStore.ts"
      via: "useBoundStore.getState() in rAF loop"
      pattern: "getState"
    - from: "src/components/AppShell.tsx"
      to: "src/components/GameCanvas.tsx"
      via: "React component composition"
      pattern: "<GameCanvas"
---

<objective>
Scaffold the complete React + Vite + TypeScript project with all dependencies, create the Zustand state layer with InputEvent types and audio slice, build the three-panel layout (game canvas + debug panel + keyboard strip), and render a static game world with placeholder visuals on the canvas.

Purpose: Establishes the entire project foundation — every subsequent plan depends on this scaffold existing. The Zustand store provides the unified event interface (AINP-03) that audio hooks will write to. The canvas, keyboard strip, and debug panel provide immediate visual feedback for audio integration testing.

Output: A running React app at localhost:5173 with a full-screen layout showing a game canvas with an L-shape path and placeholder enemies, an 88-key keyboard strip at the bottom, and a debug panel on the right (dev only). The Zustand store is ready to receive NoteOn/NoteOff events.
</objective>

<execution_context>
@/Users/daviddiaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daviddiaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React+Vite project with all dependencies, config, and types</name>
  <files>
    package.json
    vite.config.ts
    tailwind.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    index.html
    src/index.css
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
    src/lib/noteUtils.ts
    src/lib/supabase.ts
    src/stores/audioStore.ts
    src/stores/index.ts
    .env.local
    .gitignore
  </files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` in the project root to scaffold (use `.` since the directory already exists). Then install all dependencies in one pass:

    ```bash
    npm install react@^19.0.0 react-dom@^19.0.0 zustand pitchy @supabase/supabase-js@^2.47.0 clsx tailwind-merge class-variance-authority
    npm install -D tailwindcss@^4.1.18 @tailwindcss/vite@^4.1.18 @vitejs/plugin-react@^4.3.4
    ```

    Configure `vite.config.ts` per research Pattern 9: React plugin + Tailwind v4 Vite plugin + `@` path alias + COOP/COEP server headers. Plugin order: `tailwindcss()` before `react()`.

    Configure `tailwind.config.ts` per research Pattern 9: darkMode 'class', content array pointing to `./index.html` and `./src/**/*.{js,ts,jsx,tsx}`, empty theme.extend (ready for future).

    Set `src/index.css` to Tailwind v4 entry point:
    ```css
    @config "../tailwind.config.ts";
    @import "tailwindcss";
    ```

    Extend `src/vite-env.d.ts` with `ImportMetaEnv` interface adding `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`. Also add WebMIDI type declarations if not in lib (declare `navigator.requestMIDIAccess` types).

    Create `src/lib/noteUtils.ts` with three pure exported functions per research Pattern 8:
    - `frequencyToMidiNote(frequency: number): number` — `Math.round(12 * Math.log2(frequency / 440) + 69)`
    - `midiNoteToName(midi: number): string` — NOTES array lookup with octave calculation `Math.floor(midi / 12) - 1`
    - `isInPianoRange(midi: number): boolean` — returns `midi >= 21 && midi <= 108`

    Create `src/lib/supabase.ts` per research Pattern 7: import `createClient`, read `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` from `import.meta.env`, export singleton client. Use nullish coalescing for empty strings.

    Create `src/stores/audioStore.ts` per research Pattern 3:
    - Define `InputEvent` interface: `type: 'NoteOn' | 'NoteOff'`, `note: number`, `noteName: string`, `source: 'mic' | 'midi'`, `frequency?: number`, `confidence?: number`, `timestamp: number`
    - Define `AudioSlice`: `activeNotes: Set<number>`, `events: InputEvent[]`, `addEvent: (event: InputEvent) => void`
    - Define `GameSlice` stub: `gamePhase: 'idle' | 'playing' | 'paused'`
    - Compose slices into `BoundStore` using Zustand slices pattern with devtools middleware
    - Export `useBoundStore` as the main store hook
    - CRITICAL: In `addEvent`, always create new `Set` for activeNotes (reference equality for re-renders). Slice events array to last 50.

    Create `src/stores/index.ts` re-exporting everything from `audioStore.ts`.

    Create `.env.local` with empty `VITE_SUPABASE_URL=` and `VITE_SUPABASE_ANON_KEY=`. Ensure `.gitignore` includes `.env.local`.

    Set `src/App.tsx` to render `<AppShell />` (created in Task 2). Set `src/main.tsx` to mount React root on `#root` element.

    Delete the default Vite template files that are not needed: `src/App.css`, `src/assets/react.svg`, any default counter component.
  </action>
  <verify>
    Run `npm run build` — must compile without errors. Run `npm run dev` and verify localhost:5173 loads without console errors. Verify Tailwind classes work by checking that the base styles reset is applied (body has no default margin).
  </verify>
  <done>
    Project scaffolded with all dependencies installed. Vite dev server starts. TypeScript compiles. Tailwind v4 is active. Zustand store exists with InputEvent type and addEvent action. noteUtils exports three functions. Supabase client singleton exists (not wired). All config files present and correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build three-panel layout with game canvas, keyboard strip, and debug panel</name>
  <files>
    src/components/AppShell.tsx
    src/components/GameCanvas.tsx
    src/components/KeyboardStrip.tsx
    src/components/DebugPanel.tsx
  </files>
  <action>
    Create `src/components/AppShell.tsx` per research Pattern 2 layout:
    - Outer div: `flex flex-col h-screen w-screen bg-gray-950 overflow-hidden`
    - Middle row: `flex flex-1 overflow-hidden` containing `<GameCanvas />` (flex-1) and `{import.meta.env.DEV && <DebugPanel />}`
    - Bottom: `<KeyboardStrip />`
    - This creates the locked layout: game canvas (left/center) + debug panel (right) + keyboard strip (bottom)

    Create `src/components/GameCanvas.tsx` per research Patterns 1 and 2:
    - `useRef<HTMLCanvasElement>` for canvas element
    - ResizeObserver `useEffect` (Pattern 2): observe parent element, set `canvas.width` and `canvas.height` to actual pixel dimensions on resize. Set initial size on mount. Disconnect on cleanup.
    - Game loop `useEffect` (Pattern 1): `requestAnimationFrame` loop calling `drawFrame(ctx, width, height)`. Clean up with `cancelAnimationFrame`.
    - `drawFrame` function renders a static game world:
      - Dark background fill (e.g., `#1a1a2e` or similar dark blue-gray)
      - L-shape path: a wide path (40-60px) starting from top-center, going down ~60% of height, then turning right to ~80% of width, rendered as a lighter-colored filled rectangle strip. Use a path color like `#2d3748` or similar muted tone.
      - 3-4 placeholder enemy circles positioned along the path, each ~40px diameter, filled with distinct colors (red, blue, green, yellow), with note name text centered inside ("C4", "E4", "G4", "A4") in white, readable at laptop resolution.
      - Simple grid dots or subtle background pattern to show the canvas is rendering correctly.
    - The canvas element uses `className="block w-full h-full"` and sits inside a flex-1 container div.

    Create `src/components/KeyboardStrip.tsx` per research Pattern 10:
    - Read `activeNotes` from `useBoundStore((s) => s.activeNotes)`
    - Render all 88 keys (MIDI 21 = A0 through MIDI 108 = C8)
    - Layout approach: render 52 white key `<div>` elements in a flex row, then overlay 36 black key `<div>` elements using absolute positioning
    - White keys: flex-1 within the container, white background, thin gray border between keys, height ~48-64px
    - Black keys: absolute positioned, narrower (~60% of white key width), shorter (~65% of white key height), black background, z-10
    - Active state: when `activeNotes.has(midiNote)`, apply a highlight color (cyan: `bg-cyan-400` for white keys, `bg-cyan-500` for black keys) — instant on/off, no transition/animation
    - No text labels on keys (per locked decision)
    - The strip sits at the very bottom of the screen (`h-12` to `h-16` range)
    - Container: `relative bg-gray-900 border-t border-gray-700`

    Create `src/components/DebugPanel.tsx` per research Pattern 11:
    - Read `events` from `useBoundStore((s) => s.events)`
    - Fixed width: `w-72` (288px)
    - Header: "Audio Events (last 50)" label
    - Scrollable list of events, auto-scroll to bottom via `useRef` + `useEffect` on events change
    - Each event row shows: source badge (mic/midi), event type (NoteOn green, NoteOff red), note name, frequency (if present, 1 decimal), confidence (if present, as percentage), timestamp delta from first event
    - Monospace font, dark background, small text (`text-xs font-mono`)
    - Container: `flex flex-col bg-gray-900 border-l border-gray-700 overflow-hidden`
  </action>
  <verify>
    Open localhost:5173 in Chrome. Verify: (1) Full-screen layout with no scrollbars, (2) Game canvas fills left/center area showing L-shape path and colored circles with note names, (3) Debug panel visible on the right with header text, (4) Keyboard strip visible at bottom showing 88 keys with correct black/white pattern. Resize the window — canvas should resize smoothly without blur. Check that `npm run build` still compiles without errors.
  </verify>
  <done>
    Three-panel layout renders correctly at any window size >= 1366x768. Game canvas shows a static L-shape path with placeholder enemies (colored circles with note names). Keyboard strip shows 88 keys with correct piano layout (black keys overlaying white keys). Debug panel shows event log header. All components read from Zustand store. Layout uses CSS flex to fill the entire viewport.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` compiles without TypeScript errors
2. `npm run dev` starts and loads at localhost:5173 without console errors
3. Full-screen layout visible: game canvas (left/center), debug panel (right), keyboard strip (bottom)
4. Game canvas renders L-shape path with 3-4 placeholder enemy circles containing note names
5. Keyboard strip shows 88 keys with correct black/white key pattern
6. Window resize causes canvas to resize smoothly (no blur, no scrollbars)
7. Zustand devtools show `pianuki-store` with `activeNotes`, `events`, `gamePhase` state
</verification>

<success_criteria>
- A running React + Vite + TypeScript application at localhost:5173
- All dependencies installed and compiling (React 19, Zustand, Tailwind v4, pitchy, Supabase client)
- Three-panel layout fills the browser window at 1366x768+
- Static game world renders on canvas with path and placeholder enemies
- Zustand store ready to receive unified NoteOn/NoteOff events from audio hooks
- Debug panel and keyboard strip are reading from the store (wired, awaiting real events)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
